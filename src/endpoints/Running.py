#import PSScheduler

# class Running():
#     def __init__(self,):
#         self.scheduler = PSScheduler()
#         pass
    
#     def on_get(self, req, resp):
#         resp.content_type = 'text/html'
#         resp.text = self.build_running_page()

#     def on_delete(self, req, resp):
#         resp.content_type = 'text/html'
#         resp.text = self.build_running_page()
            # Redirect the user back to the running page